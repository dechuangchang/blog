import Header from '../Header/main';
import ThatMain from '../../HOC/That';

const Tpl = ThatMain((that) => {

    return (
        <div className='user'>
            <Header />
            user
        </div>
    )
})
export default Tpl
